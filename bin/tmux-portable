#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

export PORTABLE_CONFIG_ROOT="$ROOT_DIR"
export TMUX_PLUGIN_MANAGER_PATH="$ROOT_DIR/.local/share/tmux/plugins"

exec tmux -f "$ROOT_DIR/tmux/tmux.conf" "$@"
